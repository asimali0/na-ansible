
---
- name: Configure Interface
  hosts: csrv1
  tasks:
    - ios_config:
        lines: 
          - network 10.10.10.0 mask 255.255.255.0
        parents: router bgp 200
        username: cisco
        password: cisco
        authorize: yes
        auth_pass: cisco

